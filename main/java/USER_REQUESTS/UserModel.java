
/*

Класс UserModel — это DTO (Data Transfer Object, объект передачи данных), предназначенный для структурированного представления данных о пользователе в приложении.
Основная задача: инкапсулировать и передавать информацию о пользователе между различными компонентами системы — например, между UI‑тестами и API, между слоями приложения или в HTTP‑запросах к серверу.

КЛЮЧЕВЫЕ ФУНКЦИИ
Класс отвечает за:

Хранение базовых данных пользователя — содержит поля для имени, email и пароля.
Сериализацию в JSON/XML — может автоматически преобразовываться в форматы для передачи по сети (например, в теле HTTP‑запроса).
Десериализацию из JSON/XML — способен принимать данные из ответов сервера или запросов клиента и заполнять свои поля.
Упрощение передачи параметров — заменяет набор отдельных аргументов (name, email, password) на единый объект.
Типизацию данных — чётко определяет типы и структуру данных пользователя (все поля — строки).
Валидацию данных на уровне типа — компилятор проверяет соответствие типов при работе с объектом.

 */

package USER_REQUESTS;

import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;

@Data
@NoArgsConstructor
@AllArgsConstructor
public class UserModel {

    private String name;
    private String email;
    private String password;

}